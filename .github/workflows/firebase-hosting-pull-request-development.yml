# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting Preview on Dev PR
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-dev-pr
  cancel-in-progress: true
on:
  workflow_call:
jobs:
  build_and_preview:
    if: "${{ github.event.pull_request.head.repo.full_name == github.repository && (github.event.action == 'opened' || github.event.action == 'synchronize') }}"
    runs-on: ubuntu-latest
    steps:
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v1.1.1
        with: 
          credentials_json: "${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_NPM_PKG }}"
      - name: Set up gcloud Cloud SDK environment
        uses: google-github-actions/setup-gcloud@v1.1.1
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}
      - run: echo " ${{ github.head_ref }} "
      - run: |
            gcloud init
            gcloud config set jlz-portfolio
            gcloud config set artifacts/repository everything-lib
            gcloud config set artifacts/location us-central1
            gcloud gcloud artifacts print-settings npm --scope=@zwiqler94 >> .npmrc
            npx google-artifactregistry-auth
      - uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: "npm"
      - run: "npm ci"
      - run: "npm run lint:fix"
      - run: "npm run build:dev"
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_JLZ_PORTFOLIO }}"
          projectId: jlz-portfolio
          target: jlz-portfolio-dev
