name: Version Test
'on':
  - push
env:
  GH_TOKEN: '${{ secrets.GH_TOKEN }}'
  commitMsg: '${{ github.event.commits[0].message }}'
jobs:
  VERSION:
    runs-on: ubuntu-latest
    steps:
      - name: check event
        run: echo "$commitMsg"


